
#include <queue>
#include <algorithm>

using namespace std;
template<typedef Tip>
class Point{
public:
  Tip x,y;
  Point():x(0),y(0){}
  Point(Tip x,Tip y): x(x), y(y){}
};

vector<Point<int>> astar(vector<vector<bool> map, Point<int> start, Point<int> goal){


}
